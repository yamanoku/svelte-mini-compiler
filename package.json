{
  "name": "svelte-mini-compiler",
  "private": true,
  "description": "sveltejs/svelte の compiler() の仕組み（parse → analyze → transform）を参考にした、HTML解析のみのコンパイラ",
  "type": "module",
  "scripts": {
    "demo": "node src/lib/demo.ts",
    "build:app": "node src/lib/build.ts",
    "serve": "node src/lib/build.ts && node src/lib/serve.ts",
    "test": "node --test 'src/**/*.test.ts'",
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=26"
  }
}
